import React from 'react'
import TagsList from './tagsList'

const Menu = React.createClass({

    render() {
        return (
            <div className="menu">
                <TagsList />
            </div>
        )
    }
})

export default Menu
